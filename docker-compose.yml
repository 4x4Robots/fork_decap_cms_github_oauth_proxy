version: '3.8'
services:
  oauth-proxy:
    image: njfarmirm-github-oauth-proxy
    build: .
    container_name: njfarmirm-github-oauth-proxy
    restart: unless-stopped
    env_file: .env
    ports:
      - "8004:8004"
    environment:
      # - NODE_ENV=production
      - NODE_ENV=development
